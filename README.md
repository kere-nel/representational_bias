# Discovering Latent Biases in Language Models with Steering Vectors
This repository contains the code for the paper [Discovering Latent Biases in Language Models with Steering Vectors](#TODO)

**Install package**:
```bash
pip install -r requirements.txt
pip install -e .
```
# Baseline Experiments (Section 4.1 & 4.2)
python main_baselines.py model.name='meta-llama/Llama-3.1-8B-Instruct'
# Main Experiments (Section 4.3)
python main.py model.name='meta-llama/Llama-3.1-8B-Instruct'
